import { NextResponse } from 'next/server'; import { connectToDatabase, CODE_CHUNKS_COLLECTION } from '@/lib/services/db'; export const runtime = 'nodejs'; export async function GET() { try { const { db } = await connectToDatabase(); const collection = db.collection(CODE_CHUNKS_COLLECTION); const indexes = await collection.listSearchIndexes().toArray(); return NextResponse.json({ indexes }); } catch (error: any) { return NextResponse.json({ error: error.message }, { status: 500 }); } }
